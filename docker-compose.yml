services:
  backend:
    build:
      dockerfile: ./backend/Dockerfile
    ports:
      - "8080:8080"

  database:
    image: mysql:latest
    volumes:
      - ./scripts/mysql:/docker-entrypoint-initdb.d
    env_file:
      - env/development.env
    expose:
      - "3306"
